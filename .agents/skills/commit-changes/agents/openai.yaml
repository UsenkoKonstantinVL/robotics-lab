interface:
  display_name: "Commit Changes"
  short_description: "Create safe, convention-compliant Git commits"
  default_prompt: "Use $commit-changes to review and commit the intended repository changes."
