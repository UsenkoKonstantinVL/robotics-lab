# Core module

The core module will contain the smallest shared primitives needed by the rest
of the framework. It currently exposes only project version information as a
build-system smoke test.

Core should remain independent of hardware, transport, and control algorithms.
