# Control modules

Control modules produce and process commands independently of robot hardware
and simulation engines.

The [`control_tui`](control_tui/README.md) application publishes normalized
manual control commands from a terminal.
